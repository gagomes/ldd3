
FILES = nbtest load50 mapcmp polltest mapper setlevel setconsole inp outp \
	datasize dataalign netifdebug asynctest

CFLAGS = -O2 -fomit-frame-pointer -Wall

all: $(FILES)

clean:
	rm -f $(FILES) *~ core

